apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    spec:
      containers:
      - name: webserver-simple
        env:
          - name: ENV
            valueFrom:
              secretKeyRef:
                name: mysecrets
                key: ENV